// Подключаем стандартную библиотеку для работы с вводом-выводом (printf)
#include <stdio.h>

// Главная функция, с которой начинается выполнение любой программы на C
int main() {
    // Выводим строку на экран и переводим курсор на новую строку (\n)
    printf("Привет, мир!\n");
    
    // Возвращаем операционной системе код успешного завершения (0)
    return 0;
}
